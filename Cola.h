#ifndef COLA_H
#define COLA_H

 #include <iostream>
/*
class NodoMaleta
{
   private:
        int valor;
        NodoMaleta *siguiente;
        friend class Cola;
    public:
        NodoMaleta(int v, NodoMaleta *sig = NULL)
        {
        valor = v;
        siguiente = sig;
        }
};
typedef NodoMaleta *pnodomaleta;

class Cola
{
    public:
        Cola(): final(NULL), frente(NULL) {}
        ~Cola();
        void encolar(int v);
        int desencolar();
    private:
        pnodomaleta frente, final;
};
*/
#endif // COLA_H
